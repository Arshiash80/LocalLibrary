extends layout

block content
  h1= title
  p(style="font-size: 1.2em") Welcome to #[strong LocalLibrary], a very basic Express website developed as a tutorial example by #[a(href="https://github.com/Arshiash80") #[em #[strong "Arshia Shahidi"👍]]].

  h1 Dynamic content

  if error
    p Error getting "!{title}" content: 
    | !{error}
  else
    p The library has the following record counts:

    ul
      li #[strong Books:] !{data.book_count}
      li #[strong Copies:] !{data.book_instance_count}
      li #[strong Copies available:] !{data.book_instance_available_count}
      li #[strong Authors:] !{data.author_count}
      li #[strong Genres:] !{data.genre_count}